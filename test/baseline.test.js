import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises'; import { applicationStates, resourceKinds, urgencyLevels } from '../src/domain.js';
test('关怀申请样例符合资源与状态约定', async () => { const data=JSON.parse(await readFile(new URL('../fixtures/application-context.json', import.meta.url))); assert.ok(resourceKinds.includes(data.resource)); assert.ok(urgencyLevels.includes(data.urgency)); assert.ok(applicationStates.includes(data.state)); assert.ok(Date.parse(data.proofValidUntil)>0); assert.match(data.policyVersion,/^\d{4}\.\d+$/); });
